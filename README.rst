ForumSphere
--------------------------------------------------------------
Pro spuštění:

	git clone https://github.com/guqo/forum.git

K běhu aplikace:

    $ python -m venv .venv
    $ .venv\Scripts\activate
    $ pip install -r requirements.txt
    $ flask run

Toť vše!
